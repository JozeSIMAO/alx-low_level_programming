#include "main.h"
#include <stdio.h>
/**
 * main - main function
 * @argc: input
 * @argv: input
 * Return: Always (0) success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
