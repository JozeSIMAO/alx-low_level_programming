#include "main.h"

/**
 * mul - multiplies 2 integers
 * @a: to be multiplied to b
 * @b: to be multiplied to a
 * Return: Always (0) success
 */

int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
