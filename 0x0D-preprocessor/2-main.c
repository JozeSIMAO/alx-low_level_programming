#include <stdio.h>

/**
 * main - prints name of file its compiled on
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: name
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
